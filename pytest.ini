[pytest]
markers =
    smoke: fast critical-path checks, run on every commit
    regression: full functional coverage, run nightly
    api: API-layer tests
    ui: browser/Playwright tests
    unit: plain Python unit tests, no browser or network

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
